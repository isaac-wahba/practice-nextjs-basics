// PrivateRoute component, not accessible as _lib is a private folder.
// if u want it to be accessible, u can prefix the _lib with %5F instead (URL encoded _) if needed.

import React from "react";

function PrivateRoute() {
  return <div>PrivateRoute</div>;
}

export default PrivateRoute;
